<operator type=bottomup class=bu name=maxDist 
  tip="calculate the distance for all 'classes' and grouped all with a distance < 'maximum distance' "
  cmd="ga_bu_maxdist @input@ @output@ @image@ @maxdist@ @classes@" 
  <attribute name="maximum distance" cmd="@maxdist@" type=int>
  <attribute name="classes" cmd="@classes@" type=string value="ALL">
</operator>

